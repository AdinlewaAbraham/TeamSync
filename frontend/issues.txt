things to do 
if a new user automatically create workspace with project and tasks


when project is created and its is added locally to workspace object it creates a bug on the sideabe
make sidehight fixed and scrollable
project and workspace navbar layout 


aplly Authorization to accesing projects and workspaces

add skeeton loaders